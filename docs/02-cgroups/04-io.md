# 04 I/O Controller

## Goal
- Set `io.max` for a cgroup.

## Build
- Implement `cgroup-tool` support for io limits.

## Verify
```bash
# TODO: add command once implemented
```

## Notes
- We will use `lsblk` to find major:minor device numbers.
